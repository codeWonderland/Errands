
// this might act more as a model - probably have to implement client side
// if changed make sure its changed everywhere (web client and swift app)
function Task() {
    self.message = "";
    self.author = "";
    self.timeCreated = "";
    self.uid = "";
}